#
# define display information
#
name: kb_miniscrub

tooltip: |
    Scrubs long sequencing reads with MiniScrub

screenshots: []

icon: icon.png

#
# define a set of similar apps that might be useful to the user
#
suggestions:
    apps:
        related:
            [app1, app2]
        next:
            [app3, app4]

#
# Configure the display and description of parameters
#
parameters :
    parameter_1 :
        ui-name : |
            First parameter
        short-hint : |
            The first parameter
        long-hint  : |
            The first parameter that needs to be entered to drive the method. This might be the first of many.

    input_reads_ref :
        ui-name : |
            Read library (or Set) object <font color=red>*</font>
        short-hint : |
            The read library or read library set for scrubbing.

    output_reads_name :
        ui-name : |
            Output library (or Set) name
        short-hint : |
            Base name for the output read library or read library set object(s).

description : |
see: https://bitbucket.org/berkeleylab/jgi-miniscrub/src/master/